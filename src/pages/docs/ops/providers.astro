---
import DocsLayout from '../../../layouts/DocsLayout.astro';
---
<DocsLayout title="模型提供商 | 罐头喵 Canmao" description="罐头喵的多模型提供商管理" current="/Canmao-site/docs/ops/providers">
  <h1>🔧 模型提供商</h1>
  <p>罐头喵支持多个 OpenAI 兼容的模型提供商，运行期可动态增删切换，无需改配置重启。</p>

  <h2>💰 提供商购买推荐</h2>
  <p>对个人聊天和自家小群使用，推荐 <strong>Qwen Token Plan</strong>（通义千问），性价比很高：</p>
  <ul>
    <li><strong>推荐套餐</strong>：约 <strong>39 元/月</strong> 档位，日常对话够用且不心疼</li>
    <li><strong>官网</strong>：<a href="https://www.qianwenai.com/" target="_blank">https://www.qianwenai.com/</a></li>
    <li><strong>适用场景</strong>：个人 QQ 聊天、自家小群日常使用「凑合」完全足够</li>
    <li><strong>覆盖能力</strong>：token plan 提供的模型主要包括<strong>文本对话、识图</strong>类模型</li>
  </ul>
  <p>购买后可把 BaseURL / APIKey 用 <code>/provider add</code> 添加进来（token plan 的 OpenAI 兼容接口地址如下）：</p>
  <pre><code># token plan 的 OpenAI 兼容 BaseURL
https://token-plan.cn-beijing.maas.aliyuncs.com/compatible-mode/v1

/provider add qwen https://token-plan.cn-beijing.maas.aliyuncs.com/compatible-mode/v1 YOUR_API_KEY
/provider use qwen</code></pre>

  <h2>🔑 TTS / 会话模型：阿里云余额按量付费</h2>
  <p>注意 <strong>Token Plan 与阿里云余额是两套分开的计费</strong>：</p>
  <ul>
    <li>Token Plan 套餐不包含 <strong>TTS（语音）</strong>、<strong>会话模型</strong>等能力</li>
    <li>这些能力走 <strong>阿里云 DashScope</strong>，采用 <strong>余额自动扣除、按调用量计费</strong>（现用现扣）</li>
    <li><strong>推荐充值</strong>：私人自用先充 <strong>10 元</strong> 足够撑很久，属于 TTS/会话模型的专属预算</li>
  </ul>
  <blockquote>💡 具体各模型计费单价、免费额度与可用额度，以<strong>阿里云官方文档</strong>为准，务必先查询再调用，避免超额。</blockquote>
  <ul>
    <li><strong>TTS 配置</strong>：见 <code>media</code> 段（<code>tts_endpoint</code>、<code>voice</code>），填 Aliyun DashScope key</li>
    <li><strong>会话/语音模型</strong>：其在阿里云控制台按量计费、余额自动扣</li>
  </ul>

  <h2>初始提供商</h2>
  <p><code>config.yaml</code> 中的 <code>ai.*</code> 作为初始默认提供商，在首次启动时导入 <code>data/providers.json</code>。</p>

  <h2>运行时管理</h2>
  <p>在群里（或私聊主人）直接发命令：</p>
  <pre><code>/providers                        # 列出所有提供商
/provider add 我的DeepSeek https://api.deepseek.com/v1 sk-xxxx
/provider use 我的DeepSeek
/provider remove 旧提供商</code></pre>

  <h2>代理</h2>
  <p>每个提供商可单独设置代理（适合外网受限环境访问海外 API）：</p>
  <pre><code>/provider proxy 我的DeepSeek http://127.0.0.1:7890</code></pre>

  <h2>安全注意</h2>
  <blockquote>API Key 存于 <code>data/providers.json</code>，本地私有，请勿公开提交。开源部署请用配置 + 环境变量注入。</blockquote>
</DocsLayout>