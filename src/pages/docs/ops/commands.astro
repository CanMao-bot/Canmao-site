---
import DocsLayout from '../../../layouts/DocsLayout.astro';
---
<DocsLayout title="群内命令 | 罐头喵 Canmao" description="罐头喵在 QQ 群里的所有命令" current="/Canmao-site/docs/ops/commands">
  <h1>💬 群内命令</h1>
  <p>命令前缀默认为 <code>/</code>（可在 <code>bot.prefix</code> 修改）。</p>

  <h2>基础</h2>
  <table>
    <tr><th>命令</th><th>说明</th></tr>
    <tr><td><code>/help</code></td><td>帮助菜单</td></tr>
    <tr><td><code>/ai on|off|status</code></td><td>群内开关 AI / 查看状态</td></tr>
    <tr><td><code>/bot</code></td><td>bot 信息</td></tr>
    <tr><td><code>/ask …</code></td><td>让罐头喵干活（也可直接 @ 它）</td></tr>
  </table>

  <h2>会话</h2>
  <table>
    <tr><th>命令</th><th>说明</th></tr>
    <tr><td><code>/compact</code></td><td>压缩当前对话上下文</td></tr>
    <tr><td><code>/new</code></td><td>开启新会话</td></tr>
    <tr><td><code>/sessions</code></td><td>查看历史会话</td></tr>
    <tr><td><code>/resume</code></td><td>恢复某个会话</td></tr>
    <tr><td><code>/rename</code></td><td>重命名会话</td></tr>
    <tr><td><code>/delete</code></td><td>删除会话</td></tr>
    <tr><td><code>/clear</code></td><td>清除上下文</td></tr>
  </table>

  <h2>模型与提供商</h2>
  <table>
    <tr><th>命令</th><th>说明</th></tr>
    <tr><td><code>/models</code></td><td>查看可用模型列表</td></tr>
    <tr><td><code>/model</code></td><td>查看 / 切换当前模型</td></tr>
    <tr><td><code>/provider</code>/<code>/providers</code></td><td>查看提供商</td></tr>
    <tr><td><code>/provider add &lt;名称&gt; &lt;BaseURL&gt; &lt;APIKey&gt;</code></td><td>添加提供商</td></tr>
    <tr><td><code>/provider remove &lt;名称&gt; / use &lt;名称&gt;</code></td><td>移除 / 切换提供商</td></tr>
  </table>

  <h2>调度</h2>
  <table>
    <tr><td><code>/sched</code></td><td>查看 / 管理定时任务</td></tr>
  </table>

  <h2>权限</h2>
  <table>
    <tr><td><code>/grant</code></td><td>授权</td></tr>
    <tr><td><code>/ban</code></td><td>禁用</td></tr>
    <tr><td><code>/allow</code></td><td>加入免审批白名单</td></tr>
  </table>

  <h2>文件</h2>
  <table>
    <tr><td><code>/cleanfiles</code></td><td>手动清理缓存文件</td></tr>
    <tr><td><code>/sendfile &lt;文件名&gt;</code></td><td>发送文件到本群</td></tr>
  </table>

  <blockquote>💡 更多命令以 <code>/help</code> 实际输出为准。</blockquote>
</DocsLayout>