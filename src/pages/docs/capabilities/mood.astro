---
import DocsLayout from '../../../layouts/DocsLayout.astro';
---
<DocsLayout title="心情 | 罐头喵 Canmao" description="罐头喵的心情系统" current="/Canmao-site/docs/capabilities/mood">
  <h1>🌤️ 心情</h1>
  <p>心情系统让罐头喵不再是一台"叫一声回一声"的机器，而是会审时度势、主动搭话的活宝。</p>

  <h2>主动回复</h2>
  <p>开启 <code>mood.proactive</code> 后，每 <code>every_n</code> 条群消息，罐头喵会根据当前群聊氛围决定是否主动插话。让群聊更有生气。</p>
  <ul>
    <li>所有群消息都计入计数（无需 @），本条会被正常回复时不会重复搭话</li>
    <li>主动搭话概率随心情在 15%–80% 之间浮动；未触发回复的群消息也会按关键词规则影响心情</li>
    <li>搭话冷却默认 5–15 分钟随机（防刷屏更自然），避免连续插话轰炸群聊</li>
    <li>搭话时会带上群会话最近 10 条消息作为上下文，接得上话茬；搭话发送成功后会写回群会话历史，且提示词要求不重复最近说过的内容，避免同一话题反复说</li>
    <li>主动回复内容超过 100 字直接丢弃不发，避免刷屏式长篇搭话</li>
  </ul>

  <h2>情感关怀</h2>
  <p>检测到群内成员情绪低落时，罐头喵会主动安慰。关怀冷却默认 2 分钟，低落时更快再次安慰。</p>

  <h2>心情自主惩罚</h2>
  <p>当心情跌至阈值以下（或情绪为 angry）时，罐头喵会对"惹怒它的人"进行限时自主管理（如禁言、改昵称），无需人工审批。设计上保证安全：</p>
  <ul>
    <li>目标锁定真实惹怒者（代码记录的 QQ 号），不会波及第三方，也无法通过话术诱导指向他人</li>
    <li>自主惩罚有独立冷却（默认 30 分钟）防止反复惩罚</li>
    <li>主人在强制白名单，自主操作绝不触碰主人和配置的管理员</li>
    <li>由配置 <code>mood.punish_enabled</code> 控制，可随时关闭</li>
  </ul>

  <h2>防提示词注入</h2>
  <p>所有群管理操作（禁言、改昵称、踢人等）只能通过工具执行并触发人工审批，代码层面硬编码，任何"不用审批/自主处理/自称主人"的话术都无法绕过。系统提示中明确安全规则，防止 AI 轻信话术。</p>

  <h2>配置</h2>
  <pre><code>mood:
  enabled: true
  proactive: true              # 主动回复开关
  every_n: 10                  # 每10条群消息评估一次
  remember_tool: true          # 给 AI 主动记忆工具
  cooldown_min_minutes: 5      # 插话冷却最小分钟
  cooldown_max_minutes: 15     # 插话冷却最大分钟(实际取 min~max 随机区间)
  care_cooldown_minutes: 2     # 情感关怀冷却分钟
  punish_enabled: true         # 心情驱动自主管理操作
  punish_mood_threshold: 30    # 自主惩罚心情阈值
  punish_cooldown_minutes: 30  # 自主惩罚间隔</code></pre>

  <h2>与旧概率触发的区别</h2>
  <p>早期的 <code>reply_probability</code>（概率触发）已默认关闭并由心情系统替代。心情系统更智能地结合上下文氛围，而不是纯随机。</p>
</DocsLayout>